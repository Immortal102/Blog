var item = "#comments .comment_" + <%= @comment.id.to_s %>
$('#comments .edit_comment').animate({
    opacity: 0.25,
    left: "-=70",
    height: "toggle"
  }, 500, function() {
    $(item).html("<%= j (render partial: 'comments/comment') %>")
  });
